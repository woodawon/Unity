vector = 배열

deltatime, deltaposition = 시간당, 거리당 일정 수준동안의 기준값을 의미
+ deltatime동안 움직인 값이 빠르다면 높은 값으로, 느리다면 느린 값으로 나오는 것으로,
이를 동일한 값으로 주기 위해 1로 넣어 주는 것임. (상하좌우로만 움직일 수 있게 하기도 함)

Input.touchCount = 모바일 장치 화면에 접촉돼 있는 손가락의 갯수 의미
보통 if 구문을 이용하여 touchCount가 0보다 크면, 터치가 발생한 것으로 처리함.

Input.GetTouch = 모바일 장치 화면에서 접촉한 손가락의 순서에 해당하는 터치의 상태를 나타내는
Touch 구조체를 반환함. 보통 화면에 처음 접촉한 터치 정보를 이용해 동작을 처리함. => 싱글 터치라 표현.

Touch의 deltaPosition = 가장 마지막 프레임에서 발생한 터치의 위치와 현재 프레임에서 발생한 터치 위치 차이 의미
터치의 이동 방향을 추적해야 할 때 활용 가능.

TouchPhase = 가장 최근의 프레임에서 손가락이 취한 동작. 현재 터치의 상태 확인
: Began(시작), Moved(이동), Stationary(화면 터치했으나, 마지막 프레임에서 변화 없는 상태),
Ended(화면 터치를 벗어나 터치가 끝난 상태), Canceled(여러 터치 입력이 동시에 발생해서 시스템이 터치의 추적을 취소한 상태)
